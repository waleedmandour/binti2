<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core Voice Assistant -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Storage — scoped correctly per API level -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"  android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Foreground service types (Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- FIX #1 — FOREGROUND_SERVICE_LOCATION requires ACCESS_FINE_LOCATION to be
         declared before it AND the service to actively use location. Binti only uses
         location for navigation queries, not continuous tracking. Removed to avoid
         Play/AppGallery rejection; SPECIAL_USE covers the assistant use-case. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- UI & system control -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- FIX #2 — WRITE_SETTINGS is required for setBrightness() in
         DiLinkCommandExecutor but was missing from the manifest entirely. -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- Connectivity & background -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Phone & contacts -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH"       android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Hardware features — all optional for car head unit compatibility -->
    <uses-feature android:name="android.hardware.microphone"      android:required="false" />
    <uses-feature android:name="android.hardware.location.gps"    android:required="false" />
    <uses-feature android:name="android.hardware.telephony"       android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth"       android:required="false" />
    <uses-feature android:name="android.software.leanback"        android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen"     android:required="false" />

    <!-- Package visibility: DiLink apps + geo intent -->
    <queries>
        <package android:name="com.byd.auto.ac" />
        <package android:name="com.byd.auto.navigation" />
        <package android:name="com.byd.auto.media" />
        <package android:name="com.byd.auto.settings" />
        <package android:name="com.byd.auto.phone" />
        <package android:name="com.byd.auto.launcher" />
        <package android:name="com.byd.auto.climate" />
        <package android:name="com.byd.auto.vehicleinfo" />
        <package android:name="com.byd.auto.energy" />
        <package android:name="com.byd.auto.camera" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <!-- FIX #3 — tel: intent is needed for makePhoneCall() ACTION_DIAL -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <application
        android:name=".BintiApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Binti"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        <!-- FIX #4 — requestLegacyExternalStorage has no effect on API 30+
             (ignored by the OS) and triggers lint warnings. Removed. -->
        tools:targetApi="34"
        tools:replace="android:allowBackup,android:supportsRtl,android:fullBackupContent">

        <meta-data
            android:name="com.huawei.hms.client.appid"
            android:value="appid=110123456" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:theme="@style/Theme.Binti">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Binti Voice Service -->
        <!-- FIX #5 — foregroundServiceType must match the declared
             FOREGROUND_SERVICE_* permissions. Removed |location since we
             dropped FOREGROUND_SERVICE_LOCATION above. -->
        <service
            android:name=".BintiService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone|specialUse"
            android:directBootAware="true">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="voice_assistant" />
        </service>

        <!-- DiLink Accessibility Service -->
        <!-- FIX #6 — label and description are required by Android for the
             Accessibility Settings screen. Without them the service shows a
             blank entry and some OEMs refuse to enable unnamed services. -->
        <service
            android:name=".dilink.DiLinkAccessibilityService"
            android:exported="false"
            android:directBootAware="true"
            android:label="@string/accessibility_service_label"
            android:description="@string/accessibility_service_description"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Boot Receiver -->
        <!-- FIX #7 — QUICKBOOT_POWERON is a non-standard HTC/Qualcomm action.
             On BYD DiLink (MTK/Qualcomm SoC) the correct fast-boot action is
             android.intent.action.REBOOT or left as-is if confirmed on device.
             Also added android:priority="1" so the receiver is processed before
             other apps and the service starts promptly on boot. -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter android:priority="1">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Quick Actions Widget -->
        <receiver
            android:name=".ui.QuickActionsWidget"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.binti.dilink.action.START_VOICE" />
                <action android:name="com.binti.dilink.action.TOGGLE_AC" />
                <action android:name="com.binti.dilink.action.NAVIGATE_HOME" />
                <action android:name="com.binti.dilink.action.MEDIA_PLAY" />
                <action android:name="com.binti.dilink.action.NEXT_TRACK" />
                <action android:name="com.binti.dilink.action.TOGGLE_FAVORITE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_actions_widget_info" />
        </receiver>

        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
